# 持久化巩固实施计划

1. 新增巩固模型与迁移，先写会话完成后自动创建、词源范围和恢复状态测试。
2. 新增读取/推进巩固 API，并让当天会话响应携带巩固状态。
3. 改造学习页：队列完成自动进入巩固，支持闪卡状态恢复与底部进度。
4. 改造 `QuizRunner` 支持固定单词列表、起始索引和逐题推进回调，接入选择/默写恢复。
5. 跑后端测试、类型检查、Web 构建并部署生产 MySQL 与前端。
